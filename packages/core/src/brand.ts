/**
 * The name the tool answers to: the binary you type, and the prefix on every
 * line it prints.
 *
 * One constant rather than the same word spelled out in every message, because
 * a rename that reaches the manifest but misses a `console.error` leaves the
 * tool introducing itself by a name that no longer exists.
 */
export const NAME = "denji";
