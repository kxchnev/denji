{
  "name": "power-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run -w power test",
    "typecheck": "npm run -w power typecheck",
    "build": "npm run -w power build",
    "docs": "npm run -w docs dev",
    "docs:build": "npm run -w docs build"
  }
}
